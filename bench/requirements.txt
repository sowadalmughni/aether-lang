# Aether benchmark Python dependencies.
#
# langchain stack: exact pins. bench/baselines/langchain_baseline.py imports
# real langchain symbols (LCEL chains, RunnableParallel, InMemoryCache); the
# numbers in bench/results/langchain_v1.json are tied to these exact versions,
# so loosening the pins would break artifact-evaluation reproducibility.
#
# dspy_baseline.py is still a stdlib-only behavioral stub. dspy-ai stays on
# a range pin until that baseline is rewritten.

langchain==0.3.28
langchain-core==0.3.84
langchain-openai==0.2.14
langchain-anthropic==0.2.4
dspy-ai>=2.5,<3.0
datasets>=2.20,<4.0
numpy>=1.26,<3.0
scipy>=1.13,<2.0
httpx>=0.27,<1.0
